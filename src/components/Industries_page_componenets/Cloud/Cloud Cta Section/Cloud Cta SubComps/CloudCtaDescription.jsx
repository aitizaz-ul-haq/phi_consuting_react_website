import React from 'react';

const CloudCtaDescription = ({ text }) => {
    return (
        <div className="cta-descrip">
            {text}
        </div>
    );
}

export default CloudCtaDescription;
